
import numpy as np

def generate_array(n):
    np.random.choice(list("R", "G", "B"), size=n)


def sorting_func(input_list):
    for i in len(input_list):
        if input_list[i] == "R":
            
        if input_list[i] == "B":

    print(output_list)


#input_list = generate_array(50)
sorting_func(generate_array(50))
